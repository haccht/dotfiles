# .bashrc

# Source global definitions
[[ -n "$BASH_PROFILE" ]] && . ~/.bash_profile
[[ -f /etc/bashrc ]]     && . /etc/bashrc

alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'
alias ls='ls -F --color=auto'
alias grep='grep --color=auto'
